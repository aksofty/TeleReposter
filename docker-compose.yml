version: '1.0'

services:
  telereposter:
    container_name: telereposter
    build: .
    volumes:
      - './app/data/db_sources.json:/opt/TeleReposter/volumes/db_sources.json'
      - './app/logs/all_logs.log:/opt/TeleReposter/volumes/all_logs.log'
    restart: always